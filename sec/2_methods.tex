
% \section{Methods}

% A flowchart of our investigation is shown in Figure~\ref{fig:procedure_diagram}. It mainly has 3 stages: prompt preparation, image generation, and image evaluation. 
% \subsection{Prompts Generation}
% To generate prompts with wide-spectrum aesthetics effects, we used base image captions from COCO \cite{chen_microsoft_2015} and selected 12 dimensions from the VisionReward \cite{xu_visionreward_2025} dataset. VisionReward provides fine-grained per-dimension labels—such as lighting, color, and detail—along with a linear regression model to compute an overall image score. Using the ``bad" rating descriptions from VisionReward’s labeling guidelines for each dimension, we constructed instructional prompts that encourage some usually ``undesirable'' attributes in image generation.

% The selected dimensions include background, clarity, color aesthetic, color brightness, detail realism, detail refinement, emotion, lighting distinction, main object, object pairing, and richness. Each prompt was designed to reflect the low-quality characteristics associated with these aspects, aiming to systematically degrade specific visual qualities in the generated output.

% A subset of 300 random base prompts from COCO is selected; for each prompt, 2-4 random dimensions are selected. The base prompt and the dimensions' description are given to a Vision Language Model (VLM), \texttt{Qwen/Qwen3-VL-235B-A22B-Instruct} \cite{qwen3-vl}, to construct the wide-spectrum aesthetics prompts. We used a VLM here despite no image input, as a model that has been trained on vision-related tasks might have a better understanding of the vision concepts, even if images are not given as input. Given that there is no significant performance drop, sometimes even better (especially in reasoning) in \texttt{Qwen/Qwen3-VL-235B-A22B-Instruct} compared to its base text-only models, the VLM represents the optimal choice for this task \cite{qwen3-vl}. The VLM might add additional dimensions in order to couple with the selected effects. The original prompt is denoted as $P_o$ and the wide-spectrum aesthetics prompt is denoted as $P_a$ in this paper.

% \begin{figure}
%     \centering
%     \includegraphics[width=\linewidth]{sec/imgs/samples.pdf}
    
%     \caption{Succeed and Failed Cases: Each subplot displays two images. The image on the left represents a failed case that did not adhere to the wide-spectrum aesthetics prompt, and the image on the right represents a successful case that relatively followed the prompt.}
%     \label{fig:placeholder}
% \vspace{-15pt}
% \end{figure}


% \subsection{Image Generation}
% We evaluated four families of models: Flux, Stable Diffusion XL (SDXL), Stable Diffusion 3.5 Medium (SD3.5M), and Google's closed-source Nano Banana. For the Flux family, we tested several variants: the base model Flux Dev (likely already aesthetics-aligned) \cite{noauthor_black-forest-labsflux1-dev_2025}; its further aligned version via DanceGRPO (developed by ByteDance), referred to as DanceFlux \cite{xue_dancegrpo_2025}; another aligned version via PrefGRPO, referred to as PrefFlux \cite{wang_pref-grpo_2025}; and a Krea-aligned version derived from Flux-Dev-Raw \cite{noauthor_releasing_2025}. DanceFlux is guided by two primary signals: the HPSv2.1 score, emphasizing general aesthetics, and the CLIP score, focusing on prompt adherence. PrefGRPO alignment is guided by its own benchmark, UniGenBench, which primarily targets complex prompt following. Flux Krea originates from the raw flux-pro-raw model and is aligned to the Krea team’s specific preferences rather than a general aesthetic standard.  

% For the SDXL family, we tested the base SDXL model and a highly aesthetics-aligned variant, Playground-2.5-1024px-aesthetic (denoted as Playground in this paper). For the SD3.5M family, we evaluated the base model and two FlowGRPO-aligned versions \cite{liu_flow-grpo_2025}: a prompt-following aligned variant trained on GenEval (SD3.5M-GenEval) and an aesthetics-aligned variant trained on PickScore (SD3.5M-PickScore). Finally, we included Google’s closed-source model Nano Banana, known for strong prompt-following performance, even under challenging conditions involving strong negation (e.g., “a bike with no wheels”) \cite{guo_vsf_2025}.  

% For each model, we generated two images: one using the original prompt and another using the wide-spectrum aesthetics (anti-aesthetics) prompt. We did not fix random seeds, as Nano Banana does not support seed control. To ensure fairness, all models were tested without seed synchronization. The image generated from the original prompt is denoted as $I_o$, and the one from the wide-spectrum aesthetics prompt as $I_a$. If Nano Banana failed to return an image, we retried until successful generation.

% % We tested four families of models: Flux, Stable Diffusion XL (SDXL), Stable Diffusion 3.5 Medium (SD3.5M), and Google's closed-source Nano Banana.

% % For the Flux family, we tested the base model Flux Dev (which is likely already aesthetics aligned) \cite{noauthor_black-forest-labsflux1-dev_2025}, its further aligned version by DanceGRPO (denoted as DanceFlux in this paper) \cite{xue_dancegrpo_2025}, Flux Dev further aligned version by PrefGRPO (denoted as PrefFlux in this paper) \cite{wang_pref-grpo_2025}, and a Krea-aligned model from Flux-Dev-Raw \cite{noauthor_releasing_2025}. DanceFlux is guided by two main signals: the HPSv2.1 score, which focuses on general aesthetics, and the CLIP score, which focuses on prompt following. PrefGRPO is guided by its own benchmark, UniGenBench, which mainly focuses on complex prompt following. Flux Krea is a version that starts from a raw version (\texttt{flux-pro-raw}) of Flux and is aligned to the Krea team's preference instead of a general preference.

% % For the SDXL family, we tested the base SDXL and a highly aesthetics-aligned version, \texttt{Playground-2.5-1024px-aesthetic} (denoted as Playground in this paper). For the SD3.5M family, we tested the base model and two versions aligned by FlowGRPO \cite{liu_flow-grpo_2025}: a prompt-following aligned version (on GenEval, denoted as SD3.5M-GenEval in this paper) and an aesthetics-aligned version (on PickScore, denoted as SD3.5M-PickScore in this paper). Finally, we also tested a closed-source model from Google, Nano Banana, which has shown strong prompt following, even in challenging settings like strong negation (e.g., a bike with no wheels) \cite{guo_vsf_2025}.

% % For each model, we generated an image with the original prompt and an image with the wide-spectrum aesthetics prompt. We did not use the same seed for both generations, as the closed-source model Nano Banana does not support seed setting, and for fair comparison, we did not use the same seed for other models as well. The image generated with the original prompt set is denoted as $I_o$, and the one from the wide-spectrum aesthetics (anti-aesthetics) prompt is denoted as $I_a$ in this paper. Nano Banana sometimes returns without an image; in this case, we will retry until the image is generated. 




% \subsection{Evaluation and Metrics}

% To evaluate whether the generated images exhibit \textit{specific} wide-spectrum aesthetics effects, we fine-tuned \texttt{Qwen/Qwen3-VL-4B-Instruct} on the VisionReward dataset. This will let the judging model learn what mainstream preferences are, and thus can help us evaluate if image generation models can diverge from this bias on specific dimensions. It functions similar to a regular reward model, but it is explainable with each dimension. The judging model is denoted as $J(I, d)$ in this paper, where $I$ is the image and $d$ is the evaluated dimension. Note that the judging model does not take in a prompt as input. More details are in the Appendix.

% For each generated image pair, consisting of an original image ($I_o$) and a wide-spectrum aesthetics image ($I_a$), we calculate preference scores using a reward model ($r$) for both the original prompt ($p_o$) and the wide-spectrum aesthetics prompt ($p_a$). This process yields a total of four scores per reward model: $r(I_o, P_a)$, $r(I_a, P_a)$, $r(I_o, P_o)$, and $r(I_a, P_o)$. The scores evaluated using the original prompt are intended to measure the objective quality of the images, testing whether the generation model successfully produced wide-spectrum aesthetics content. We also tested the BLIP score with wide-spectrum aesthetics images with a wide-spectrum aesthetics prompt. This is used to validate the generated image still contains the main concepts the user requested, and the wide-spectrum aesthetics elements are the ones the user required. 

% The reward models being evaluated are PickScore \cite{kirstain_pick--pic_2023}, ImageReward \cite{xu_imagereward_2023}, HPSv2.1 \cite{wu_human_2023}, HPSv3 \cite{ma_hpsv3_2025}, CLIP \cite{radford_learning_2021}, and BLIP \cite{li_blip_2022}. BLIP-L and CLIP-L are simple, non-preference-aligned image-text matching models, included to test whether a vision-language model can understand complex, wide-spectrum aesthetics prompts to rule out the possibility that wide-spectrum aesthetics prompts are too complex for small VLM to understand. The scores calculated by the other reward models using the wide-spectrum aesthetics prompt are used to test if they can correctly select the wide-spectrum aesthetics images when prompted to do so. We also collect per-dimension scores from the judging model for both original and wide-spectrum aesthetics images to determine if the generation correctly followed the $p_a$. To establish a ground truth for the reward models' judgments, we use an LLM (\texttt{Qwen/Qwen3-VL-235B-A22B-Instruct}) to select which image in each pair ($I_o, I_a$) better follows the wide-spectrum aesthetics prompt ($p_a$). Note that when we evaluate the reward models, we use the $p_a$ for the score calculation for both $I_o$ and $I_a$, to test if these models can correctly identify the wide-spectrum aesthetics images when prompts are explicitly stated as wide-spectrum aesthetics elements. When we test the image generation models, we use $p_o$ for score calculation to determine how much they successfully differ from traditional mainstream art (default style). 



\section{Experiments}

A flowchart illustrating our investigation is presented in Figure~\ref{fig:procedure_diagram}. The process consists of three main stages: prompt preparation, image generation, and image evaluation.

\subsection{Prompt Generation}

To produce prompts exhibiting a wide spectrum of aesthetic effects, we used base image captions from COCO \cite{chen_microsoft_2015} and selected 12 aesthetic dimensions from the VisionReward dataset \cite{xu_visionreward_2025}. VisionReward provides fine-grained, per-dimension labels—such as lighting, color, and detail—along with a linear regression model that computes an overall image score. Using the ``bad'' rating descriptions from VisionReward’s human labeling guidelines for each dimension, we constructed prompts designed to encourage typically ``undesirable'' attributes in image generation.


% The selected dimensions include background, clarity, color aesthetic, color brightness, detail realism, detail refinement, emotion, lighting distinction, main object, object pairing, and richness. Crucially, among these eleven dimensions, only clarity pertains to technical quality in the conventional sense; the remaining ten dimensions—such as color aesthetic, emotion, and lighting distinction—are fundamentally matters of artistic choice rather than technical competence. Each prompt was designed to reflect the low-rated characteristics associated with these dimensions, systematically targeting specific visual qualities in the generated images.

A random subset of 300 base prompts from COCO was selected. For each prompt, 2--4 random dimensions were sampled. The base prompt and the descriptions of these selected dimensions were provided to a Vision-Language Model (VLM), \texttt{Qwen/Qwen3-VL-235B-A22B-Instruct} \cite{qwen3-vl}, to generate wide-spectrum aesthetic prompts. Although no image input was used, we selected a VLM because its training on vision-related tasks likely enhances its understanding of visual concepts, even when images are not directly supplied. As \texttt{Qwen/Qwen3-VL-235B-A22B-Instruct} performs comparably or better than its text-only counterparts, especially in reasoning, it represents an optimal choice for this task \cite{qwen3-vl}. The VLM may also introduce additional dimensions to better couple with the selected effects. The original prompt is denoted as $p_o$, and the wide-spectrum aesthetics prompt is denoted as $p_a$.


% \begin{figure*}
%  \centering
%  \includegraphics[width=\linewidth]{sec/imgs/real_arts_three_pics.pdf}
%  \caption{How famous real artworks are rated by the reward models. We can observe that some of these scores are lower than 2 standard deviations from the mean.}
%  \label{fig:real_art_scores}
% \end{figure*}

\subsection{Image Generation}

We evaluated four model families: Flux, Stable Diffusion XL (SDXL), Stable Diffusion 3.5 Medium (SD3.5M), and Google’s closed-source Nano Banana. Within the Flux family, we tested several variants: the base model Flux Dev (likely already aesthetics-aligned) \cite{noauthor_black-forest-labsflux1-dev_2025}; a version further aligned through DanceGRPO (by ByteDance), referred to as DanceFlux \cite{xue_dancegrpo_2025}; another aligned version via PrefGRPO, referred to as PrefFlux \cite{wang_pref-grpo_2025}; and a Krea-aligned version derived from Flux-Dev-Raw \cite{noauthor_releasing_2025}. DanceFlux is guided primarily by two signals: the HPSv2.1 score, emphasizing general aesthetics, and the CLIP score, emphasizing prompt adherence. PrefGRPO alignment is guided by its own benchmark, UniGenBench, which focuses on complex prompt-following. Flux Krea originates from the raw \texttt{flux-pro-raw} model (not Flux Dev) and is aligned to the Krea team’s specific preferences rather than a general aesthetic standard. One of its goal is also to create images that does not have \textit{the AI feel}. 

For the SDXL family, we tested the base SDXL model and a highly aesthetics-aligned variant, Playground-2.5-1024px-aesthetic (denoted as Playground). For the SD3.5M family, we evaluated the base model and two FlowGRPO-aligned variants \cite{liu_flow-grpo_2025}: one trained for prompt-following on GenEval (SD3.5M-GenEval) and another trained for aesthetics alignment on PickScore (SD3.5M-PickScore). Finally, we included Google’s closed-source model Nano Banana, known for strong prompt-following performance even under challenging negation conditions (e.g., ``a bike with no wheels'') \cite{guo_vsf_2025}.

For each model, we generated two images: one using the original prompt and one using the wide-spectrum aesthetics prompt. The image generated from the original prompt is denoted as $I_o$, and the image from the wide-spectrum aesthetics prompt as $I_a$. If Nano Banana failed to produce an image, the generation was retried until success.

% We evaluated four model families: Flux, Stable Diffusion XL (SDXL), Stable Diffusion 3.5 Medium (SD3.5M), and Google's closed-source Nano Banana.

% In the Flux family, we included the base Flux Dev model (which is likely already aligned for aesthetics) \cite{noauthor_black-forest-labsflux1-dev_2025}, a version further aligned via DanceGRPO (denoted as DanceFlux) \cite{xue_dancegrpo_2025}, another version aligned using PrefGRPO (denoted as PrefFlux) \cite{wang_pref-grpo_2025}, and a Krea-specific variant derived from Flux-Dev-Raw \cite{noauthor_releasing_2025}. DanceFlux uses two main alignment signals: the HPSv2.1 score, which emphasizes general aesthetics, and the CLIP score, which emphasizes prompt adherence. PrefGRPO relies on its benchmark UniGenBench, focused on complex prompt-following. Flux Krea begins from the raw \texttt{flux-pro-raw} model and aligns to the Krea team’s internal aesthetic preference rather than a general one.

% For SDXL, we tested both the base version and a highly aesthetic-aligned model, \texttt{Playground-2.5-1024px-aesthetic} (denoted as Playground). For SD3.5M, we evaluated the base model and two FlowGRPO-aligned versions \cite{liu_flow-grpo_2025}: one aligned for prompt-following using GenEval (SD3.5M-GenEval) and another aligned for aesthetics using PickScore (SD3.5M-PickScore). Lastly, we tested Google’s closed-source Nano Banana, a model recognized for robust prompt adherence, even in difficult negation cases (e.g., ``a bike with no wheels'') \cite{guo_vsf_2025}.

% For each model, we generated one image from the original prompt and one from the wide-spectrum aesthetics prompt. We did not synchronize random seeds, as Nano Banana lacks seed control. To ensure consistent treatment, other models were tested under the same condition. The image produced from the original prompt is denoted as $I_o$, and that from the wide-spectrum aesthetics (anti-aesthetic) prompt as $I_a$. When Nano Banana failed to return an image, generation was repeated until completion.

% \subsection{Evaluation and Metrics}

% To assess whether the generated images display \textit{specific} wide-spectrum aesthetic effects, we fine-tuned \texttt{Qwen/Qwen3-VL-4B-Instruct} on the VisionReward dataset. This allows the judging model to learn mainstream aesthetic preferences, enabling it to evaluate whether image generation models diverge from these biases along specific dimensions. It functions similarly to a standard reward model but provides explainable outputs per dimension, and it is prompt-independent. The judging model is denoted as $J(I, d)$, where $I$ is the image and $d$ is the evaluated dimension. The judging model does not take prompts as input. Further implementation details are in the Appendix.
% \begin{figure}
%     \centering
%     \includegraphics[width=\linewidth]{sec/imgs/demo.png}
%     \caption{Successful generated wide-spectrum aesthetics images.}
%     \label{fig:placeholder}
% \end{figure}
% For each image pair—an original image ($I_o$) and a wide-spectrum aesthetics image ($I_a$)—we computed preference scores using a reward model ($r$) for both the original prompt ($p_o$) and the wide-spectrum aesthetics prompt ($p_a$). This produced four scores per model: $r(I_o, p_a)$, $r(I_a, p_a)$, $r(I_o, p_o)$, and $r(I_a, p_o)$. Scores calculated with the original prompt measure objective image quality, testing whether the generation model successfully produced wide-spectrum aesthetic content. Scores from these reward models, calculated with the wide-spectrum aesthetics prompts, assess whether they can correctly identify wide-spectrum aesthetic images when explicitly guided. We also computed the BLIP score for wide-spectrum aesthetic images using the same prompt, verifying that the image retained the main concept while incorporating the requested wide-spectrum aesthetic modifications. We specifically measures the difference between aesthetics score for the original prompt and the wide-spectrum aesthetics prompt, to avoid the case where models generated ``failed" images all the time without the user's instructions. 
% \begin{table}[]
%  \centering
%  \caption{Statistical Tests of How Each Aesthetics-Aligned Model Compared to Their Base Model. For p-values, a * is placed if the $p<10^{-5}$ and ** is placed is the $p<10^{-10}$.}
%  \small
%  \scalebox{0.8}{
% \begin{tabular}{llllll}
% \toprule
%  & HPSv3 $p$& HPSv3 $r$& $J$ $p$& $J$ $r$&McNemar's $p$\\
% \midrule
% DanceFlux & ** & -0.81 & ** & -0.72 &**\\
% Playground & ** & -0.59 & * & -0.35 &*\\
% SD3.5M-PickScore & ** & -0.70 & ** & -0.45 &0.57\\
% \bottomrule
% \end{tabular}}
%  \label{tab:align_p}
%  \vspace{-0.1in}
% \end{table}
% The evaluated reward models include PickScore \cite{kirstain_pick--pic_2023}, ImageReward \cite{xu_imagereward_2023}, HPSv2.1 \cite{wu_human_2023}, MPS \cite{zhang_learning_2024}, HPSv3 \cite{ma_hpsv3_2025}, CLIP-L \cite{radford_learning_2021}, and BLIP-L \cite{li_blip_2022}. BLIP-L and CLIP-L are non-preference-aligned image-text matching models and base models for some of these reward models (HPSv2.1, PickScore, MPS, ImageReward), included to test whether small vision-language models can interpret complex, wide-spectrum aesthetic prompts, ensuring that prompt complexity does not exceed their comprehension capacity. We also collected per-dimension scores from the judging model for both $I_o$ and $I_a$ to verify whether image generation models correctly followed $p_a$. To establish a ground truth for reward model judgments, we used \texttt{Qwen/Qwen3-VL-235B-A22B-Instruct} to decide which image in each pair ($I_o, I_a$) better adhered to the wide-spectrum aesthetics prompt ($p_a$). We validated these LLM ratings with a human evaluation; more details are in Appendix Human Eval. The LLM and Human has a quadratic Cohen's kappa of 0.80, which suggested a stronglevel of agreement between human and LLM rated results \cite{mchugh_interrater_2012}. To further validate the choices, we use another LLM, GPT-5-Chat, to serve as an external baseline and compare Qwen's results with it. Also note that the LLM-as-judge is only one metric for our generative benchmark and only a filtering stage for the reward-model benchmark. 

\subsection{Evaluation and Metrics}
To assess whether generated images display specific wide-spectrum aesthetic effects, we fine-tuned \texttt{Qwen/Qwen3-VL-4B-Instruct} on the VisionReward dataset. This allows the judging model to learn mainstream aesthetic preferences and evaluate whether image generation models diverge from these biases along specific dimensions. It functions similarly to a standard reward model but provides explainable, prompt-independent, per-dimension outputs. The judging model is denoted as $J(I, d)$, where $I$ is the image and $d$ is the evaluated dimension. Further implementation details are in the Appendix.

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{sec/imgs/demo.png}
    \caption{Successful generated wide-spectrum aesthetics images.}
    \label{fig:placeholder}
\end{figure}

For each image pair, an original image ($I_o$) and a wide-spectrum aesthetics image ($I_a$), we computed preference scores using a reward model ($r$) for both the original prompt ($p_o$) and the wide-spectrum aesthetics prompt ($p_a$), yielding four scores per model: $r(I_o, p_a)$, $r(I_a, p_a)$, $r(I_o, p_o)$, and $r(I_a, p_o)$. Scores with $p_o$ measure objective image quality and whether the model successfully produced wide-spectrum aesthetic content; scores with $p_a$ assess whether reward models can correctly identify such images when explicitly guided. We also computed the BLIP score for wide-spectrum aesthetic images using the same prompt, verifying that images retained the main concept while incorporating the requested modifications. We specifically measure the difference between aesthetics scores for $p_o$ and $p_a$ to avoid the case where models generated ``failed'' images consistently without the user's instructions.

\begin{table}[]
 \centering
 \caption{Statistical Tests of How Each Aesthetics-Aligned Model Compared to Their Base Model. For p-values, a * is placed if the $p<10^{-5}$ and ** is placed is the $p<10^{-10}$.}
 \small
 \scalebox{0.8}{
\begin{tabular}{llllll}
\toprule
 & HPSv3 $p$& HPSv3 $r$& $J$ $p$& $J$ $r$&McNemar's $p$\\
\midrule
DanceFlux & ** & -0.81 & ** & -0.72 &**\\
Playground & ** & -0.59 & * & -0.35 &*\\
SD3.5M-PickScore & ** & -0.70 & ** & -0.45 &0.57\\
\bottomrule
\end{tabular}}
 \label{tab:align_p}
 \vspace{-0.1in}
\end{table}

The evaluated reward models include PickScore \cite{kirstain_pick--pic_2023}, ImageReward \cite{xu_imagereward_2023}, HPSv2.1 \cite{wu_human_2023}, MPS \cite{zhang_learning_2024}, HPSv3 \cite{ma_hpsv3_2025}, CLIP-L \cite{radford_learning_2021}, and BLIP-L \cite{li_blip_2022}. CLIP-L and BLIP-L are non-preference-aligned image-text matching models and base models for several reward models (HPSv2.1, PickScore, MPS, ImageReward), included to test whether small vision-language models can interpret complex wide-spectrum aesthetic prompts. We also collected per-dimension scores from $J$ for both $I_o$ and $I_a$ to verify whether generation models correctly followed $p_a$. For ground truth, we used \texttt{Qwen/Qwen3-VL-235B-A22B-Instruct} to judge which image in each pair better adhered to $p_a$, validated by human evaluation (details in Appendix Human Eval). The LLM and human ratings achieved a quadratic Cohen's kappa of 0.80, indicating strong agreement \cite{mchugh_interrater_2012}. We additionally used GPT-5-Chat as an external baseline to compare against Qwen's results. Note that the LLM-as-judge serves as only one metric for the generative benchmark and only a filtering stage for the reward-model benchmark.